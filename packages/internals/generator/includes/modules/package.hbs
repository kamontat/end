{
  "name": "{{create_module_name category name}}",
  "private": "{{#if (is_type category)}}true{{else}}false{{/if}}",
  "version": "0.1.0",
  "description": "{{#if (is_type category)}}TypeScript definitions for {{get_normal_module_name name}}{{else}}{{description}}{{/if}}",{{#if (is_type category)}}{{else}}
  "main": "lib/index.js",{{/if}}
  "typings": "lib/index.d.ts"{{#if (is_type category)}}{{else}},
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "{{#if (is_prod category)}}nmsys-compiler-webpack{{else}}inmsys-tsc{{/if}}"{{#if (is_prod category)}},
    "clean": "nmsys-cleaner",
    "test": "nmsys-tester"
    {{else}}{{/if}}
  },
  "dependencies": {},
  "devDependencies": {
    {{#if (is_type category)}}
    "@nmsys/stack-commandline": "0.2.1",
    "@nmsys/stack-config-eslint": "0.2.0",
    "@nmsys/stack-config-jest": "0.2.1",
    "@nmsys/stack-config-prettier": "0.2.0",
    "@nmsys/stack-config-webpack": "0.2.0"
    {{else}}
    "@nmsys/internal-compiler": "0.1.1"
    {{/if}}
  }{{/if}}
}
